version: '3.8'

services:
  smartbin-hrm:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: smartbin-hrm-microservice
    ports:
      - "3004:80"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
