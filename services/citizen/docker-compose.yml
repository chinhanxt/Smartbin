version: '3.8'

services:
  smartbin-citizen-portal:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: smartbin-citizen-service
    ports:
      - "3002:80"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
